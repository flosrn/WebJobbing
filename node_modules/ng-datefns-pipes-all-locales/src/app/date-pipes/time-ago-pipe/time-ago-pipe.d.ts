import { PipeTransform, OnDestroy } from '@angular/core';
import 'rxjs/add/observable/interval';
import 'rxjs/add/operator/startWith';
import { DatePipeManager } from '../providers/date-pipe-manager';
import { LocaleManager } from '../providers/locale-manager/locale-manager';
export declare class TimeAgoPipe implements PipeTransform, OnDestroy {
    private localeManager;
    private refresher;
    private refreshNow;
    private previousAgoExpression;
    private options;
    constructor(manager: DatePipeManager, localeManager: LocaleManager);
    ngOnDestroy(): void;
    transform(inputTime: string | number | Date): string;
    private agoExpression(inputTime);
}
