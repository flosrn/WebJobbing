import { InjectionToken } from '@angular/core';
export interface AgoPipeOptions {
    refreshRate?: number;
    prefix?: string;
    suffix?: string;
    includeSeconds?: boolean;
}
export interface DatePipeConfiguration {
    defaultDateFormat?: string;
    dateDistanceIncludesSeconds?: boolean;
    agoPipeOptions?: AgoPipeOptions;
}
export declare const DATE_PIPE_CONFIGURATION_TOKEN: InjectionToken<DatePipeConfiguration>;
export declare class DatePipeManager {
    private dateFormat;
    private formatToRevertTo;
    private includeSeconds;
    private agoPipeOptions;
    private optionsToRevertTo;
    constructor(userProvidedConfig: DatePipeConfiguration);
    private initializeDefaultDateFormat(defaultFormat);
    getDefaultFormat(overrideFormat?: string): string;
    setDefaultFormat(newFormat: string): void;
    resetDefaultFormat(): void;
    includeSecondsInDistance(): void;
    doNotIncludeSecondsInDistance(): void;
    secondsIncludedInDistance(): boolean;
    setAgoPipeOptions(options: AgoPipeOptions): void;
    getAgoPipeOptions(): AgoPipeOptions;
    resetAgoPipeOptions(): void;
}
