import { InjectionToken } from '@angular/core';
import { Locale } from './locale';
export declare const DEFAULT_LOCALE_TOKEN: InjectionToken<Locale>;
export declare class LocaleManager {
    private defaultLocale;
    private localeToRevertTo;
    constructor(userProvidedLocale: Locale);
    setDefaultLocale(newDefaultLocale: Locale): void;
    getDefaultLocale(): Locale;
    getFullLocale(): any;
    resetDefaultLocale(): void;
}
